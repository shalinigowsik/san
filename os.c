#include<stdio.h>
int main()
{
int a,b,rem,i;
printf("give the first number for the range:|n");
scanf("%d,&a);
printf("give the final number for the range :|n");
scanf("%d",n&b);
printf("|n the even number between %d and %d are ",a,b);
for (i=a;i<=b;++i)
{
rem=i%2;
if(rem==0)
printf("\n%d",i);
}
return o;
}
